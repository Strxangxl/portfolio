import HeroSection from "@/components/HeroSection";
import { Box } from "@chakra-ui/react";

const Home = () => {
  return (
    <Box marginTop={-24}>
      <HeroSection />
    </Box>
  );
};

export default Home;
